# 🧠 اتصال و تحلیل داده با MongoDB و Python در پروژه‌های علم داده

این پروژه نحوه‌ی اتصال، مدیریت و تحلیل داده‌ها با استفاده از **MongoDB** و **Python (PyMongo)** را در قالب یک گردش‌کار علمی داده (Data Science Workflow) نشان می‌دهد.  
در این پروژه، مثال‌های واقعی از **درج داده، ایجاد ایندکس، انجام کوئری‌ها، عملیات به‌روزرسانی و تحلیل با Aggregation Pipeline** پیاده‌سازی شده‌اند.  
محیط‌های هدف شامل **Kaggle Notebooks** و **MongoDB Atlas** هستند.

---

## 📊 معرفی پروژه

MongoDB یک پایگاه داده‌ی NoSQL انعطاف‌پذیر و قدرتمند است که برای ذخیره‌ی داده‌های بزرگ و غیرساخت‌یافته در پروژه‌های مدرن علم داده کاربرد فراوان دارد.  
در این مخزن (Repository)، تمامی مراحل به‌صورت گام‌به‌گام پیاده‌سازی شده‌اند:

- ایجاد و اتصال پایگاه داده (محلی و اطلس)
- درج داده‌ها به‌صورت تکی یا دسته‌ای
- ایجاد ایندکس برای افزایش سرعت جستجو
- اجرای کوئری‌ها و فیلترها
- انجام عملیات به‌روزرسانی و حذف
- اجرای **Aggregation Pipeline** برای تحلیل داده‌ها
- اتصال به `pandas` جهت تحلیل و تجسم نتایج

---

## 🧩 داده‌ها و هدف پروژه

در این پروژه، یک مجموعه‌داده‌ی مصنوعی (Synthetic Dataset) برای شبیه‌سازی داده‌های فروش در چند دسته و منطقه مختلف ایجاد شده است.  
ما از **دو کالکشن (Collection)** برای نمایش قابلیت‌های تحلیلی MongoDB استفاده می‌کنیم:

1. `sales_data` → شامل اطلاعات محصولات (دسته، قیمت، تعداد، درآمد)
2. `region_sales` → شامل خلاصه‌ی فروش بر اساس منطقه و سال

🎯 هدف این پروژه نمایش قدرت **Aggregation Framework** در MongoDB است که می‌تواند جایگزین بسیاری از Queryهای پیچیده SQL شود.

---

## ⚙️ مراحل اجرایی

| مرحله | توضیح |
|--------|--------|
| 1️ | اتصال به MongoDB (محلی یا Atlas) با استفاده از `pymongo` |
| 2️ | ایجاد پایگاه داده و کالکشن به‌صورت پویا |
| 3️ | درج داده‌ها از DataFrame‌های pandas |
| 4️ | ساخت ایندکس‌های تکی و ترکیبی برای بهینه‌سازی جستجو |
| 5️ | اجرای فیلترها و به‌روزرسانی‌های شرطی |
| 6️ | پیاده‌سازی Aggregation Pipeline برای تحلیل گروهی |
| 7️ | استخراج و تجسم داده‌ها با `pandas` و `matplotlib` |

---

## 📂 ساختار پوشه‌ها


📦 MongoDB_Python_DataScience/
├── notebooks/        → نوت‌بوک‌های کگل برای تحلیل و آموزش
├── src/              → کدهای ماژولار (db_connect, data_ops, aggregations)
├── assets/           → تصاویر و گراف‌های نمونه برای README
├── results/          → خروجی‌ها، نمودارها و فایل‌های نتایج
├── models/           → مدل‌های ذخیره‌شده (در صورت وجود)
├── configs/          → فایل‌های تنظیمات پروژه
├── logs/             → گزارش‌ها و لاگ‌های ارتباط با پایگاه داده
├── README.md         → معرفی انگلیسی پروژه
├── README_FA.md      → نسخه فارسی معرفی پروژه
├── requirements.txt  → کتابخانه‌های مورد نیاز
├── .gitignore        → مسیرهای حذف‌شده از ریپو
└── CONTRIBUTING.md   → راهنمای مشارکت در پروژه


---

## 🧰 نصب و اجرا

### ۱️ کلون کردن پروژه:

```
git clone https://github.com/<your-username>/MongoDB_Python_DataScience.git
cd MongoDB_Python_DataScience
```

### ۲️ نصب پیش‌نیازها:

```
pip install -r requirements.txt
```

### ۳️ پیکربندی MongoDB:

شما می‌توانید از یکی از دو روش زیر استفاده کنید:

* **نسخه محلی MongoDB** → `mongodb://localhost:27017/`
* **MongoDB Atlas (Cloud)** → جایگزین کردن URI در فایل تنظیمات

---

## 📈 نتایج و مصورسازی

MongoDB با استفاده از Aggregation Pipeline قابلیت اجرای تحلیل‌های پیچیده را تنها با چند خط کد دارد:

```python
pipeline = [
    {"$group": {"_id": "$category", "total_revenue": {"$sum": "$revenue"}, "avg_price": {"$avg": "$price"}}},
    {"$sort": {"total_revenue": -1}}
]
```

✅ نمونه خروجی:

| دسته        | مجموع درآمد | میانگین قیمت |
| ----------- | ----------- | ------------ |
| Electronics | 54000       | 360          |
| Furniture   | 31000       | 210          |
| Home        | 25000       | 175          |

📊 نتایج نهایی با استفاده از `matplotlib` و `pandas` مصورسازی می‌شوند.

---

## 🔗 نوت‌بوک کاگل

می‌توانید نسخه‌ی کامل پیاده‌سازی را در کاگل مشاهده کنید:
**[👉 لینک پروژه در Kaggle](**(https://www.kaggle.com/code/sepehrkh/mongodb-and-python-on-kaggle-using-mongodb-atlas)**)**


---

## 🚀 مسیرهای توسعه‌ی آینده

* استفاده از **MongoDB Atlas Search** برای جستجوی متنی
* اتصال MongoDB به **PySpark** برای تحلیل داده‌های کلان
* ساخت داشبورد **Streamlit/Dash** برای نمایش بلادرنگ داده‌ها

---

## 🧾 مجوز

این پروژه تحت مجوز **MIT License** منتشر شده است.
استفاده و توسعه برای اهداف آموزشی و پژوهشی آزاد است.

---

## 📬 راه ارتباطی

برای پرسش‌ها و همکاری‌ها:

**Kaggle:**[@sepehrkh](https://www.kaggle.com/sepehrkh)
**GitHub:** [@sepehr-khb](https://github.com/sepehr-khb)
**LinkedIn:** [Sepehr Khalili](https://www.linkedin.com/in/sepehr-khalilib/)
